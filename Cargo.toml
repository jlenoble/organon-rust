[workspace]
members = [
    "packages/flatten_iterator",
    "packages/kilo",
    "packages/minigrep",
    "packages/organon",
    "packages/sandbox",
    "packages/webserver",
]
exclude = ["packages/rustos"]

[workspace.dependencies]
color-eyre = { version = "0.6.2" }
